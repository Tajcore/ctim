name: my-app
region: fra1
services:
  - name: django-app
    github:
      repo: your-github-username/your-repo-name
      branch: main
      deploy_on_push: true
    environment_slug: python
    build_command: "docker build -t my-django-app -f ./compose/test_integration/django/Dockerfile ."
    run_command: "/start"
    http_port: 8000
    instance_size_slug: basic-xxs
    instance_count: 1
    envs:
      # Define necessary environment variables here

databases:
  - name: ctim-int-db-postgresql-fra1-08372
    engine: pg
    version: "15"
    size: db-s-1vcpu-1gb
    region: fra1
    num_nodes: 1
    tags:
      - test_env
      - integration
